const readline = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout
});

readline.question('Введите название страницы: ', str => {
    readline.question('Введите второе значение: ', text => { 
        for(let i = 0; i < Array.length; i++){
            int = Array[{
                Имя:str,
                Фамилия:text
            }];
        console.log(i); 
        
readline.close();
        }
    });
});
